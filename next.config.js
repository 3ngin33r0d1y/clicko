/** @type {import('next').NextConfig} */
const nextConfig = {
  server: {
    // Set the IP address to your machine's IP or '0.0.0.0' to listen on all available network interfaces.
    host: "0.0.0.0",
  },
};

module.exports = nextConfig;
